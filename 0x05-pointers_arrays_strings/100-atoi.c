#include "main.h"
#include <stdlib.h>

/**
 * _atoi - convert a string to integer
 *
 * @s: the string to be converted
 *
 * Return: the integer value
*/

int _atoi(char *s)
{
	int num = atoi(s);

	return (num);
}
