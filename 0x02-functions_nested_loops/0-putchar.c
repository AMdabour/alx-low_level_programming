#include "main.h"

/**
 * main - entry point
 *
 * Return: ALways 0 (success)
*/

int main(void)
{
	char wor[] = "_putchar";
	int car;

	for (car = 0, car < 8, car++)
	{
		_putchar(wor[]);
	}
	_putchar('\n');
	return (0);
}
